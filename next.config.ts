import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Local images live under public/images — add remotePatterns here if you use external URLs again */
};

export default nextConfig;
